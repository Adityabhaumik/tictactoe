import "package:flutter/material.dart";

const Color screenBackGroundColor = Color(0xFF142F43);
const Color buttonColor = Color(0xFFFFAB4C);
enum Turn { player1, player2, notPlaying ,unPlayable }
enum boardStatus {player1,player2,empty}
